{
  "name": "utxo-vm-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Universal Chain-Agnostic Smart Object Engine for UTXO Blockchains",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present",
    "asbuild": "npm run asbuild --workspace=@utxo-vm/contracts"
  },
  "devDependencies": {
    "assemblyscript": "^0.27.29",
    "typescript": "^5.3.3"
  }
}
